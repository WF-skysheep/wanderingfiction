{
  "name": "ocr-deepseek-translator",
  "version": "1.2.0",
  "private": true,
  "description": "Cloudflare Pages frontend + standalone Worker backend for DeepSeek OCR translation",
  "scripts": {
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy .",
    "worker:dev": "wrangler dev --config wrangler.worker.toml",
    "worker:deploy": "wrangler deploy --config wrangler.worker.toml",
    "worker:wf:migrate:local": "wrangler d1 execute wf-db --config wrangler.worker.toml --local --file worker-backend/migrations/001_wf.sql",
    "worker:wf:migrate:remote": "wrangler d1 execute wf-db --config wrangler.worker.toml --remote --file worker-backend/migrations/001_wf.sql"
  },
  "devDependencies": {
    "wrangler": "^4.38.0"
  }
}
